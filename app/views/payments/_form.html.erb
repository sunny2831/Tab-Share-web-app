<%= form_with(model: payment, local: true) do |form| %>
  <% if payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tab_id %>
    <%= form.number_field :tab_id %>
  </div>

  <div class="field">
    <%= form.label :paying_user_id %>
    <%= form.number_field :paying_user_id %>
  </div>

  <div class="field">
    <%= form.label :submitting_user_id %>
    <%= form.number_field :submitting_user_id %>
  </div>

  <div class="field">
    <%= form.label :payment_amount %>
    <%= form.text_field :payment_amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
